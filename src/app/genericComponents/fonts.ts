import { Montserrat, Silkscreen, Lora } from "next/font/google";

export const montserrat = Montserrat({ subsets: ["latin"] });
export const silkscreen = Silkscreen({
  subsets: ["latin"],
  weight: "400",
});
export const lora = Lora({ subsets: ["latin"] });
